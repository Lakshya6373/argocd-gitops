apiVersion: v2
name: sample-app
description: >
  Helm chart for sample-app. Demonstrates per-environment value files used
  with ArgoCD's Helm source type. Deploy using ArgoCD Application spec with
  spec.source.helm.valueFiles pointing to the appropriate values-<env>.yaml.

type: application
version: 0.1.0
appVersion: "1.0.0"

keywords:
  - sample
  - gitops
  - argocd

maintainers:
  - name: platform-team
    email: platform@example.com
